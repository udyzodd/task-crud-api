// textContent, innerText, innerHTML

// const element = document.querySelector('#demo');
// let htmlContent = element.innerHTML;
// let pureText = element.textContent;
// let visibleText = element.innerText;

// console.log(htmlContent); // returns the html content of the element
// console.log(pureText); // returns the text content of the element
// console.log(visibleText); // returns the visible text content of the element

// element.innerHTML = `<i>${element.innerHTML}</i>`;
// console.log(element);

// ----

// setAttribute, getAttribute and removeAttribute

// let p = document.querySelector('a');
// p.setAttribute('href', 'https://www.x.com'); // set attribute of an element to something else
// alert(p.getAttribute('href')); // get the value of the attribute of an element
// p.removeAttribute('href'); // remove the original value of the element

// ---

// createElement, appendChild, removeChild, prepend